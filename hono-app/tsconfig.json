{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "skipLibCheck": true,

    // ✅ Add these two 
    "lib": ["ESNext", "DOM", "WebWorker"],  // this line allows me to use console.log otherwise it is not recognizing the console

    "jsx": "react-jsx",
    "jsxImportSource": "hono/jsx"
  }
}
